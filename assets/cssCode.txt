:root {
    --textColor: #22577A;
    --backColor: #80ED99;
}

* {
    margin: 0;
    padding: 0;
    font-size: 100%;
    font-family: serif;
    color: var(--textColor);
}

body {
    height: 100vh;
    display: grid;
    place-items: center;
    background-color: var(--backColor);
}

h1 {
    font-size: 5rem;
}

p {
    font-size: 2.5rem;
}